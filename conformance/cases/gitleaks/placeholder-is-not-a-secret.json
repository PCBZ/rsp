{ "name": "placeholder-is-not-a-secret", "clause": "V2", "plugin": "gitleaks",
  "note": "AKIAIOSFODNN7EXAMPLE is the key AWS prints in its own documentation, and gitleaks allowlists anything ending EXAMPLE. The adapter must inherit that judgement rather than reach its own: a wrapper whose verdicts differ from the tool it wraps is not wrapping it. This case exists because a hand-written regex got it wrong.",
  "request": { "rsp_version": "0.1", "hook": "on_chunk", "content": "aws_access_key: AKIAIOSFODNN7EXAMPLE" },
  "expect": { "response": { "verdict": "ALLOW" } } }
