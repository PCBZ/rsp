{ "name": "error-noisy-diagnostics-are-not-an-error", "clause": "E3", "kind": "host",
  "note": "More diagnostics than the host keeps, and a valid answer on stdout. The excess is thrown away — that is what E3 means by a failure costing only diagnostics — and the answer is still the answer. Blocking here would make every debug build a refusal. max_output is small so the case reaches the truncation path rather than stopping short of it.",
  "max_output": 4096,
  "plugin": { "behaviour": "noisy", "reply": { "verdict": "FLAG", "reason": "seen", "severity": "low" } },
  "request": { "rsp_version": "0.1", "hook": "on_chunk", "content": "ordinary text" },
  "expect": { "verdict": "FLAG", "content": "unchanged" } }
