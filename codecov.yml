# A signal, not a gate: the protocol's health is clause coverage (SPEC.md §10),
# and line coverage only says how well this one host is tested.
coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

comment:
  layout: "condensed_header, files"
  require_changes: true
